name: memory-falsification

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  memory-falsification:
    runs-on: ubuntu-latest

    services:
      redis:
        image: redis/redis-stack:latest
        ports:
          - 6379:6379
      qdrant:
        image: qdrant/qdrant:latest
        ports:
          - 6333:6333
          - 6334:6334

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0

      - name: Cache Cargo artifacts
        uses: Swatinem/rust-cache@v2

      - name: Wait for Redis and Qdrant
        run: |
          python3 - <<'PY'
          import socket
          import time

          targets = [
              ("127.0.0.1", 6379, "redis"),
              ("127.0.0.1", 6334, "qdrant-grpc"),
          ]

          deadline = time.time() + 90
          for host, port, name in targets:
              while True:
                  try:
                      with socket.create_connection((host, port), timeout=2):
                          print(f"{name} is up on {host}:{port}")
                          break
                  except OSError:
                      if time.time() > deadline:
                          raise SystemExit(f"Timed out waiting for {name} on {host}:{port}")
                      time.sleep(1)
          PY

      - name: Run memory API falsification tests
        env:
          MNEMO_TEST_REDIS_URL: redis://localhost:6379
          MNEMO_TEST_QDRANT_URL: http://localhost:6334
          MNEMO_TEST_QDRANT_PREFIX: mnemo_
        run: cargo test -p mnemo-server --test memory_api -- --test-threads=1
